<?php

declare(strict_types=1);

namespace {{ namespace }};

use CakephpFixtureFactories\Factory\BaseFactory as CakephpBaseFactory;
use CakephpFixtureFactories\Generator\GeneratorInterface;
{% if useStatements %}
{% for useStatement in useStatements %}
use {{ useStatement }};
{% endfor %}
{% endif %}

/**
 * {{ factory }}
 *
 * @extends \CakephpFixtureFactories\Factory\BaseFactory<{{ entityClass }}>
 */
class {{ factory }} extends CakephpBaseFactory
{
    /**
     * Defines the Table Registry used to generate entities with
     *
     * @return string
     */
    protected function getRootTableRegistryName(): string
    {
        return '{{ rootTableRegistryName }}';
    }

    /**
     * Defines the factory's default values. This is useful for
     * not nullable fields. You may use methods of the present factory here too.
     *
     * @return array<string, mixed>
     */
    public function definition(GeneratorInterface $generator): array
    {
        return [
            {{ FactoryBake.defaultData(defaultData)|raw }}
        ];
    }
{% for association, associationData in toOne %}

    /**
     * @param \Cake\Datasource\EntityInterface|callable|array|string|int|null $parameter
     *
     * @return static
     */
    public function for{{ association }}($parameter = null): static
    {
        return $this->for({{ associationData.className }}::new($parameter));
    }
{% endfor %}
{% for association, associationData in oneToMany %}

    /**
     * @param int $n
     * @param \Cake\Datasource\EntityInterface|callable|array|string|int|null $parameter
     *
     * @return static
     */
    public function has{{ association }}(int $n = 1, $parameter = null): static
    {
        return $this->has({{ associationData.className }}::new($parameter)->count($n));
    }
{% endfor %}
{% for association, associationData in manyToMany %}

    /**
     * @param int $n
     * @param \Cake\Datasource\EntityInterface|callable|array|string|int|null $parameter
     *
     * @return static
     */
    public function has{{ association }}(int $n = 1, $parameter = null): static
    {
        return $this->has({{ associationData.className }}::new($parameter)->count($n)->without('{{ modelName }}'));
    }
{% endfor %}
}
