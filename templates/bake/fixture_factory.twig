<?php

declare(strict_types=1);

namespace {{ namespace }};

use Cake\Datasource\EntityInterface;
use CakephpFixtureFactories\Factory\BaseFactory as CakephpBaseFactory;
use CakephpFixtureFactories\Generator\GeneratorInterface;
{% if useStatements %}
{% for useStatement in useStatements %}
use {{ useStatement }};
{% endfor %}
{% endif %}

/**
 * {{ factory }}
 *
 * @method {{ entityClass }} getEntity()
 * @method array<{{ entityClass }}> getEntities()
 * @method {{ entityClass }}|array<{{ entityClass }}> persist()
 * @method static {{ entityClass }} get(mixed $primaryKey, array $options = [])
 */
class {{ factory }} extends CakephpBaseFactory
{
    /**
     * Defines the Table Registry used to generate entities with
     *
     * @return string
     */
    protected function getRootTableRegistryName(): string
    {
        return '{{ rootTableRegistryName }}';
    }

    /**
     * Defines the factory's default values. This is useful for
     * not nullable fields. You may use methods of the present factory here too.
     *
     * @return void
     */
    protected function setDefaultTemplate(): void
    {
        $this->setDefaultData(function (GeneratorInterface $generator) {
            return [
                {{ FactoryBake.defaultData(defaultData)|raw }}
            ];
        });
    }
{% for association, associationData in toOne %}

    /**
     * @param \Cake\Datasource\EntityInterface|callable|array|string|int|null $parameter
     *
     * @return static
     */
    public function with{{ association }}($parameter = null): static
    {
        if (is_numeric($parameter)) {
            $factory = {{ associationData.className }}::make()->times((int)$parameter);
        } elseif ($parameter instanceof EntityInterface) {
            $factory = {{ associationData.className }}::makeFrom($parameter);
        } elseif (is_callable($parameter)) {
            $factory = {{ associationData.className }}::makeWith($parameter);
        } else {
            $factory = {{ associationData.className }}::make($parameter);
        }

        return $this->with(
            '{{ association }}',
            $factory,
        );
    }
{% endfor %}
{% for association, associationData in oneToMany %}

    /**
     * @param \Cake\Datasource\EntityInterface|callable|array|string|int|null $parameter
     * @param int $n
     *
     * @return static
     */
    public function with{{ association }}($parameter = null, int $n = 1): static
    {
        if (is_numeric($parameter)) {
            $factory = {{ associationData.className }}::make()->times((int)$parameter);
        } elseif ($parameter instanceof EntityInterface) {
            $factory = {{ associationData.className }}::makeFrom($parameter)->times($n);
        } elseif (is_callable($parameter)) {
            $factory = {{ associationData.className }}::makeWith($parameter)->times($n);
        } else {
            $factory = {{ associationData.className }}::make($parameter)->times($n);
        }

        return $this->with(
            '{{ association }}',
            $factory,
        );
    }
{% endfor %}
{% for association, associationData in manyToMany %}

    /**
     * @param \Cake\Datasource\EntityInterface|callable|array|string|int|null $parameter
     * @param int $n
     *
     * @return static
     */
    public function with{{ association }}($parameter = null, int $n = 1): static
    {
        if (is_numeric($parameter)) {
            $factory = {{ associationData.className }}::make()->times((int)$parameter);
        } elseif ($parameter instanceof EntityInterface) {
            $factory = {{ associationData.className }}::makeFrom($parameter)->times($n);
        } elseif (is_callable($parameter)) {
            $factory = {{ associationData.className }}::makeWith($parameter)->times($n);
        } else {
            $factory = {{ associationData.className }}::make($parameter)->times($n);
        }

        $factory = $factory->without('{{ modelName }}');

        return $this->with(
            '{{ association }}',
            $factory,
        );
    }
{% endfor %}
}
