<?php

declare(strict_types=1);

namespace {{ namespace }};

use CakephpFixtureFactories\Factory\BaseFactory as CakephpBaseFactory;
use CakephpFixtureFactories\Generator\GeneratorInterface;
{% if toOne or oneToMany or manyToMany %}
use InvalidArgumentException;
{% endif %}
{% if useStatements %}
{% for useStatement in useStatements %}
use {{ useStatement }};
{% endfor %}
{% endif %}

/**
 * {{ factory }}
 *
 * @extends \CakephpFixtureFactories\Factory\BaseFactory<{{ entityClass }}>
 */
class {{ factory }} extends CakephpBaseFactory
{
    /**
     * Defines the Table Registry used to generate entities with
     *
     * @return string
     */
    protected function getRootTableRegistryName(): string
    {
        return '{{ rootTableRegistryName }}';
    }

    /**
     * Defines the factory's default values. This is useful for
     * not nullable fields. You may use methods of the present factory here too.
     *
     * @return array<string, mixed>
     */
    public function definition(GeneratorInterface $generator): array
    {
        return [
            {{ FactoryBake.defaultData(defaultData)|raw }}
        ];
    }
{% for association, associationData in toOne %}

    /**
     * @param \Cake\Datasource\EntityInterface|callable|array|string|null $parameter
     *
     * @throws \InvalidArgumentException
     *
     * @return static
     */
    public function for{{ association }}($parameter = null): static
    {
        if (is_int($parameter)) {
            throw new InvalidArgumentException('for{{ association }}() does not accept integer payloads. Pass an entity, array, string, callable or null.');
        }

        return $this->with('{{ association }}', {{ associationData.className }}::new($parameter));
    }
{% endfor %}
{% for association, associationData in oneToMany %}

    /**
     * @param int $n
     * @param \Cake\Datasource\EntityInterface|callable|array|string|null $parameter
     *
     * @throws \InvalidArgumentException
     *
     * @return static
     */
    public function has{{ association }}(int $n = 1, $parameter = null): static
    {
        if (is_int($parameter)) {
            throw new InvalidArgumentException('has{{ association }}() does not accept integer payloads as the second argument. Pass entity/array/string/callable data there; use the first argument for the count.');
        }

        return $this->with('{{ association }}', {{ associationData.className }}::new($parameter)->count($n));
    }
{% endfor %}
{% for association, associationData in manyToMany %}

    /**
     * @param int $n
     * @param \Cake\Datasource\EntityInterface|callable|array|string|null $parameter
     *
     * @throws \InvalidArgumentException
     *
     * @return static
     */
    public function has{{ association }}(int $n = 1, $parameter = null): static
    {
        if (is_int($parameter)) {
            throw new InvalidArgumentException('has{{ association }}() does not accept integer payloads as the second argument. Pass entity/array/string/callable data there; use the first argument for the count.');
        }

        return $this->with('{{ association }}', {{ associationData.className }}::new($parameter)->count($n)->without('{{ modelName }}'));
    }
{% endfor %}
}
